{
  "name": "copiloting-agents",
  "version": "1.2.0",
  "description": "Real-time web dashboard for monitoring GitHub Copilot agent sessions",
  "type": "module",
  "workspaces": ["server", "client"],
  "bin": {
    "copiloting-agents": "./bin/cli.js"
  },
  "files": [
    "bin",
    "server/dist",
    "client/dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/vmagalhaes/copiloting-agents.git"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "concurrently --names server,client --prefix-colors cyan,magenta \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=server && npm run build --workspace=client",
    "install:all": "npm install",
    "start": "npm run build && node server/dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
